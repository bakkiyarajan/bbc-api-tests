{
  "name": "bbc-api-tests",
  "version": "1.0.0",
  "description": "API Automation Task",
  "type": "module",
  "scripts": {
    "test": "cucumber-js --format json:report/cucumber_report.json",
    "report": "node util/report.js",
    "test:full": "npm run test && npm run report"
  },
  "dependencies": {
    "chai": "^4.3.6",
    "supertest": "^6.3.3"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.3.0",
    "cucumber-html-reporter": "^7.2.0"
  }
}
